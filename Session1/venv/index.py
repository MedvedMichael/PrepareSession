# Variant from Пивной чай)
import functions

mainMatrix = functions.generateMatrix()
functions.printMatrix(mainMatrix)

functions.findMaxsAndMoveColumns(mainMatrix)
functions.printMatrix(mainMatrix)

mLines = functions.getLines(mainMatrix)

